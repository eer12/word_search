# 文档搜索系统使用说明

## 1. 安装文档搜索系统

### 1.1 安装wheel文件

1. **准备工作**
   - 确保已安装Python 3.7或更高版本
   - 确保pip已更新到最新版本
   - 确保项目根目录下存在`dist`文件夹，并且包含`document_search_system-1.0-py3-none-any.whl`文件

2. **安装命令**
   在命令提示符(cmd)中执行以下命令：
   ```bash
   pip install dist/document_search_system-1.0-py3-none-any.whl
   ```

3. **安装验证**
   安装完成后，可以通过以下命令验证安装是否成功：
   ```bash
   pip list | findstr document-search-system
   ```
   如果看到`document-search-system`包，则表示安装成功。

## 2. 运行文档搜索系统

### 2.1 启动系统

1. **使用脚本启动（推荐）**
   - 双击 `scripts\服务开始.vbs` 文件
   - 服务会在后台运行，即使关闭窗口也不会影响服务运行
   - 启动成功后会显示提示信息

2. **在命令提示符中运行**
   直接在cmd中输入以下命令：
   ```bash
   document-search
   ```

3. **访问系统**
   系统启动后，在浏览器中访问：
   ```
   http://localhost:3000
   ```

4. **停止系统**
   - 使用脚本停止（推荐）：双击 `scripts\服务停止.vbs` 文件
   - 在命令提示符中按`Ctrl+C`组合键停止系统

### 2.2 使用脚本文件

**脚本文件说明：**
- `scripts\服务开始.vbs`：在后台启动文档搜索系统服务
- `scripts\服务停止.vbs`：停止运行中的文档搜索系统服务

**使用方法：**
1. 双击 `scripts\服务开始.vbs` 启动服务
2. 服务启动后，在浏览器中访问 `http://localhost:3000`
3. 当需要停止服务时，双击 `scripts\服务停止.vbs`

**脚本运行特点：**
- 服务在后台运行，不占用命令行窗口
- 即使关闭启动窗口，服务仍会继续运行
- 停止脚本会自动查找并终止服务进程

## 3. 数据库存放位置

### 3.1 数据库类型

文档搜索系统使用SQLite数据库，这是一种文件型数据库，不需要额外安装数据库服务。

### 3.2 数据库文件位置

数据库文件的位置取决于您运行`document-search`命令时的当前工作目录：

- **相对路径**：系统使用`'documents.db'`作为数据库文件路径
- **具体位置**：数据库文件会在您执行`document-search`命令的目录中创建

**不同运行方式的数据库位置：**

1. **使用脚本启动**
   - 当双击 `scripts\服务开始.vbs` 时，脚本会在 `scripts` 目录的父目录（即项目根目录）中运行
   - 数据库文件会创建在：`C:\Users\ADMIN\Documents\trae_projects\word_wearch\documents.db`

2. **在命令提示符中运行**
   - 数据库文件会在您执行命令时的当前目录创建
   - 例如：
     - 如果在`C:\Users\ADMIN\Documents`目录下运行命令，数据库文件会在`C:\Users\ADMIN\Documents\documents.db`
     - 如果在`D:\`目录下运行命令，数据库文件会在`D:\documents.db`

### 3.3 确认数据库位置

1. **查看当前工作目录**
   在运行`document-search`命令的终端中，先执行：
   ```bash
   cd
   ```
   或
   ```bash
   echo %cd%
   ```
   查看当前目录路径

2. **检查数据库文件**
   在该目录中查找`documents.db`文件：
   ```bash
   dir documents.db
   ```

3. **验证数据库内容**
   可以使用SQLite工具（如SQLite Browser）打开该文件查看数据库结构和内容。

## 4. 系统功能

### 4.1 主要功能

- **文件上传**：支持上传PDF、Word、WPS和TXT等格式的文档
- **分类管理**：可以创建、编辑和删除文档分类
- **全文搜索**：支持精确匹配和模糊搜索
- **文件管理**：可以查看、下载和删除文档
- **系统设置**：可以修改上传目录等系统设置

### 4.2 支持的文件格式

- `.pdf`：PDF文档
- `.docx`、`.doc`：Word文档
- `.wps`：WPS文档
- `.txt`：文本文件

## 5. 常见问题及解决方案

### 5.1 安装失败

**问题**：安装wheel文件时出现错误

**解决方案**：
- 确保pip版本是最新的：`pip install --upgrade pip`
- 确保系统已安装所有依赖项
- 检查wheel文件是否完整

### 5.2 命令未找到

**问题**：运行`document-search`命令时提示"命令未找到"

**解决方案**：
- 检查Python的Scripts目录是否在系统PATH中
- 重启命令提示符
- 尝试使用完整路径运行：`python -m document_search_system`

### 5.3 数据库连接失败

**问题**：系统启动时提示数据库连接失败

**解决方案**：
- 检查当前目录是否有写权限
- 确保磁盘空间充足
- 检查数据库文件是否被其他程序占用

### 5.4 文件上传失败

**问题**：上传文件时失败

**解决方案**：
- 检查文件大小是否超过系统限制
- 确保上传目录存在且有写权限
- 检查文件格式是否被支持

## 6. 技术细节

### 6.1 数据库结构

- **categories**：存储分类信息
- **documents**：存储文档基本信息
- **document_categories**：存储文档与分类的关联
- **file_content_fts**：使用SQLite FTS5实现全文检索

### 6.2 依赖项

- Flask：Web框架
- PyPDF2：PDF文件处理
- python-docx：Word文件处理
- pytesseract：OCR文字识别
- Pillow：图像处理
- pdf2image：PDF转图像
- waitress：生产服务器
- jieba：中文分词
- mammoth：Word文档转换
- olefile：OLE文件处理
- docx2txt：DOCX转文本
- pywin32：Windows系统API

## 7. 系统维护

### 7.1 备份数据库

定期备份`documents.db`文件到安全位置，以防数据丢失。

### 7.2 清理文件

定期清理上传目录中的临时文件和不需要的文档，保持系统性能。

### 7.3 更新系统

当有新版本时，先卸载旧版本，再安装新版本：
```bash
pip uninstall document-search-system
pip install dist/document_search_system-1.0-py3-none-any.whl
```

---

**注意**：本系统仅用于内部文档管理和搜索，请勿用于生产环境或处理敏感信息。